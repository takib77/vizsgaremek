import { BooleanSignPipe } from './boolean-sign.pipe';

describe('BooleanSignPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanSignPipe();
    expect(pipe).toBeTruthy();
  });
});
